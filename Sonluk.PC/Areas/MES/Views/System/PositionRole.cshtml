@{   
    ViewBag.Title = "岗位组维护";
    Layout = "~/Views/Shared/_Layout_CRM.cshtml"; 
}

@using Sonluk.PC.Areas.MES.Models;
@model Sonluk.PC.Areas.MES.Models.MODLEDataGather

<script type="text/html" id="bar">
    <a class="layui-btn layui-btn-xs" lay-event="edit">修改</a>
    <a class="layui-btn layui-btn-xs layui-btn-danger" lay-event="delete">删除</a>
</script>

<script type="text/html" id="indexTpl">
    {{d.LAY_TABLE_INDEX+1}}
</script>



<span class="layui-breadcrumb" lay-separator=">">
    <a>MES系统设置</a>
    <a><cite>岗位组维护</cite></a>
</span>
<br />
<br />

<div id="from1" class="layui-form layui-form-pane">
    <div id="div1" class="layui-form">

        <div class="layui-form-item">
            <button id="btn_insert" class="layui-btn">新增</button>
            <button id="btn_cx" class="layui-btn">查询</button>
        </div>

        <div class="layui-form-item">

            <label class="layui-form-label">岗位组：</label>
            <div class="layui-input-inline">
                <select id="cx_gw" lay-filter="cx_gw">
                    <option value="0" selected="selected">请选择</option>
                    @for (int i = 0; i < Model.Role_assemble_select.MES_ROLE_ASSEMBLE.Length; i++)
                    {
                        <option value="@Model.Role_assemble_select.MES_ROLE_ASSEMBLE[i].ID">@Model.Role_assemble_select.MES_ROLE_ASSEMBLE[i].ROLENAME</option>
                    }
                </select>
            </div>
        </div>

        <table class="layui-hide" id="tb_gw" lay-filter="tb_gw"></table>
    </div>
</div>

<div id="div_gw" class="layui-form" hidden>
    <br />
    @*<div class="layui-form-item">
        <label class="layui-form-label" style="width:130px">工厂：</label>
        <div class="layui-input-inline">
            <input id="gc_xg" type="text" class="layui-input" disabled>
        </div>
    </div>*@
    <div class="layui-form-item">
        <label class="layui-form-label" style="width:130px">工厂：</label>
        <div class="layui-input-inline">
            <select id="gc_xg" lay-filter="gc_xg">
                <option value="" selected="selected">请选择</option>
                @for (int i = 0; i < Model.Sy_gc.Length; i++)
                {
                    <option value="@Model.Sy_gc[i].GC">@Model.Sy_gc[i].GC-@Model.Sy_gc[i].GCMS</option>
                }
            </select>
        </div>
    </div>
    <br />
    <div class="layui-form-item">
        <label class="layui-form-label" style="width:130px">岗位组名称：</label>
        <div class="layui-input-inline">
            <input id="gname_xg" type="text" autocomplete="off" class="layui-input" placeholder="不可为空">
        </div>
    </div>
    <br />
    <fieldset class="layui-elem-field layui-field-title" style="margin-top: 10px;">
        <legend>岗位工种维护</legend>
    </fieldset>
    <table id="result_gw" lay-filter="result_gw"></table>
</div>

<div id="div_xz" class="layui-form" hidden>
    <br />

    <div class="layui-form-item">
        <label class="layui-form-label" style="width:130px">工厂：</label>
        <div class="layui-input-inline">
            <select id="gc" lay-filter="gc">
                <option value="" selected="selected">请选择（必选）</option>
                @for (int i = 0; i < Model.Sy_gc.Length; i++)
                {
                    <option value="@Model.Sy_gc[i].GC">@Model.Sy_gc[i].GC-@Model.Sy_gc[i].GCMS</option>
                }
            </select>
        </div>
    </div>

    <br/>
    <div class="layui-form-item">
        <label class="layui-form-label" style="width:130px">岗位组名称：</label>
        <div class="layui-input-inline">
            <input id="gname" type="text" autocomplete="off" class="layui-input" placeholder="不可为空">
        </div>
    </div>

</div>

@section scripts {
    @Scripts.Render("~/bundles/MES/System/PositionRole")
}