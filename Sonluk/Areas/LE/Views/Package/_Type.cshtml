@using Sonluk.UI.Model.LE.TRA.PackageService
@model IEnumerable<PackageInfo>
<div class="drop-list-body">
    <ul>
        @foreach (PackageInfo type in Model)
        {
            <li>
                <div class="drop-list-item">@type.Type</div>
                <input type="hidden" value="@type.TypeID">
            </li>
        }
    </ul>
</div>